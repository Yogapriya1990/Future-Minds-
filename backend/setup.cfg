[tool:pytest]
testpaths = tests

[coverage:run]
source = app
omit =
    app/seed.py
    alembic/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise NotImplementedError
    if TYPE_CHECKING:
